# 0.1.0 release review

- Review date: 2026-09-05.
- Package: `mosh-client` 0.1.0, MIT OR Apache-2.0.
- Implementation baseline: `94f13225aba535c6645a9179e0ce9f00b156629e`.
- Status: software, security, compatibility, and package gates passed;
  publication awaits maintainer authorization and registry authentication.
  No registry upload, tag, or GitHub Release has been created for this candidate.

## Release decision

The maintainer authorized preparation of 0.1.0 and a minimum Rust version of
1.88. The previous manifest claimed 1.85, but the reachability path uses let
chains and fails to compile on 1.85. [Rust 1.88](https://blog.rust-lang.org/2025/06/26/Rust-1.88.0/)
stabilizes that syntax. Test
exactly 1.88.0 and current stable; no higher minimum or dependency update is
needed without new evidence.

This batch changes release metadata and documentation. Raising the declared
minimum also enables current Clippy's let-chain suggestions. Two nested
conditions in the Session waiter now use equivalent let chains, preserving
poll order, short-circuiting, and return values. Protocol behavior, public API,
dependency versions, and resource limits retain the implementation baseline.
The separate fuzz package remains unpublished and its path dependency tracks
the root package's exact version.

The release scope is stock `mosh-server` 1.4.0, IPv4, a fixed server endpoint,
external SSH bootstrap, and UTF-8 `xterm-256color` presentation. See
[compatibility](../compatibility.md) and [release notes](../../CHANGELOG.md).
LeanTTY's formal 1.6 acceptance is a separate product gate.

## Consumer evidence

The retained consumer records close MCRS-001, 002, 003, 005, and 006. MCRS-004
is closed under the warning-only silent-peer contract; MCRS-008 is closed under
the visible-screen and consumer-owned restoration contract. MCRS-007 defers
IPv6, which is outside 0.1.0.

- [ARM64 prediction](../fixtures/leantty-arm64-prediction.md): `e1346b3`,
  public output in 1 ms, including during complete UDP loss.
- [Physical WLAN recovery](../fixtures/leantty-physical-local-send-recovery.md):
  `94f1322`, about 9.7 seconds offline, same Session and remote PTY after recovery.
- [Physical network switch](../fixtures/leantty-physical-network-switch.md):
  changed client address and route, retained Session and remote PTY, and a new
  command after recovery; SSH required reconnection in the comparison.

These are development test-HAP observations on one HAD-W32 PC, not fresh
device tests of the 0.1.0 package or certification of LeanTTY's formal candidate.
The detailed consumer artifacts remain outside this package. This review
imports their sanitized, revision-bound records and reruns the library gates.

## Verification

All commands ran in the default Ubuntu WSL distribution at
`/mnt/c/repos/mosh-client-rs` on 2026-09-05. The checked source contains the two
equivalent waiter changes described above.

| Gate | Result |
| --- | --- |
| Rust 1.88.0 format, strict Clippy, ordinary suite | Passed; 136 tests, 23 stock tests intentionally ignored in this suite |
| Stable Rust 1.96.0 format, strict Clippy, ordinary suite | Passed; 136 tests, 23 stock tests intentionally ignored in this suite |
| Strict rustdoc and doctest command on both compilers | Passed; no doctest examples are currently defined |
| All stock 1.4.0 fixtures, serial execution | 23 passed, including close, prediction, recovery, and two-Session isolation |
| Root and fuzz `cargo deny` and `cargo audit` | Passed; 59 and 50 registry packages respectively, checksums present, registry entries identical to the baseline |
| Nightly parser and state-transition fuzz checks and runs | Both passed; 10,000 runs per target, maximum input length 4,096 bytes |
| Release library build for `aarch64-unknown-linux-ohos` | Passed on stable; target build evidence, not a new device test |
| Source secret-pattern scan, project `unsafe`, and local Markdown links | No findings; project unsafe prohibition retained |
| Cargo package assembly and rebuild | Passed using current Cargo; unpacked package also compiled on Rust 1.88.0 |
| Clean-commit `cargo publish --locked --dry-run` | Passed; upload skipped; final commit and archive identity are in the local handoff |

The initial stable lint run exposed the two newly applicable let-chain lints;
the final run above follows their correction. An initial Cargo 1.88 archive
operation failed while reading the newly created archive's filesystem metadata.
Current Cargo assembled and verified the archive successfully; Rust 1.88 then
built that unpacked archive with `--locked --offline`. The release uses current
Cargo for packaging and tests the minimum compiler against the actual package.

The only active checklist is [the roadmap](../roadmap.md#current-work-prepare-010).
Local logs and structured results stay in `target/release-0.1.0/`, outside the
package. The final clean-commit dry run and archive identity are retained in
that directory's publication handoff.

## Security and supply chain

The accepted [OCB3 exception](../decisions/0004-rustcrypto-ocb3-security-exception.md)
continues to apply: the primitive lacks an independent audit and complete
key-derived-state zeroization. The project has not added a cipher fallback or
claimed that a dependency advisory scan resolves this residual risk.

The source scan is a bounded pattern check, not proof against every possible
secret encoding. Registry versions and checksums match the previously reviewed
dependency graph, so its accepted unsafe boundaries continue to apply. The only
Rust diff is the reviewed waiter syntax; no cryptographic or secret-handling
code changed.

GitHub private vulnerability reporting was enabled and read back as enabled on
2026-09-05. [SECURITY.md](../../SECURITY.md) links to that private channel.

## Publication handoff

The root manifest now permits only the crates.io registry. The separate fuzz
package remains `publish = false`. Retain a clean candidate commit and an
inspected Cargo package; record its commit, archive SHA-256, file list, and
dry-run result in the local handoff. The planned immutable tag is `v0.1.0` at
that exact commit.

Actual publication requires maintainer authorization. Use the verified commit
and lockfile for the crates.io upload and matching GitHub Release; never
replace a published version or move its tag. A dry run proves package assembly
and compilation, not registry account authorization or a completed upload.
The default WSL publishing environment had no configured crates.io token or
user credential provider at the 2026-09-05 preflight. Registry login is an
outstanding maintainer action; no credential was printed, copied, or installed.

LeanTTY currently pins the old Git revision with `version = "=0.0.0"`. Its
later upgrade must change both the full revision (or registry source) and the
version requirement to 0.1.0, then run its own affected acceptance gates.
